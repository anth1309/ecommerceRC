<main class="container">
	<section class="row">
		<div class="col-12 ">{{ form_start(categoryForm) }}
		</div>
		<div class="col-12 my-3">{{ form_row(categoryForm.name,  { attr: { 'class': 'form-control' }}) }}
		</div>
		<div class="col-12 my-3">{{ form_row(categoryForm.parent, { attr: { 'class': 'form-control' }}) }}
		</div> 		
		<div class="col-12 my-3">
		<button type="submit" class="btn btn-primary">AJOUTER</button>
	    </div>
		{{ form_end(categoryForm) }}
    </section>
</main>